export interface Cliente {
    id: number;
    nome: string;
    email: string;
    telefone: string;
}

export interface ClienteFormValues {
    nome: string;
    email: string;
    telefone: string;
}